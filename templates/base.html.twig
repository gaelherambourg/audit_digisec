<!DOCTYPE html>
<html>
	<head>
		{#
						    Meta :
						    encodage UTF-8 HTML5
						    Responsive
						    informations aux vieux navigateurs web IE
						    #}
		<meta charset="UTF-8">
		<meta name="description" content="Application de réalisation d'audits"/>
		<meta name="viewport" content="width=device-width, user-scalable=yes, initial-scale=1.0">
		<meta http-equiv="X-UA-Compatible" content="ie=edge">
		<title>
			{% block title %}Digisec
			{% endblock %}
		</title>
		{# Run `composer require symfony/webpack-encore-bundle`
						           and uncomment the following Encore helpers to start using Symfony UX #}
		{% block stylesheets %}
			{#
						            CSS normalize
								    CSS
								    framework BULMA
								    Foontawsome
								    #}
			<link rel="stylesheet" href="{{ asset('css/normalize.css') }}">
			<link rel="stylesheet" type="text/css" href="{{ asset('css/style.css') }}">
			<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
			<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
		{% endblock %}
		{% block javascripts %}
			{#{{ encore_entry_script_tags('app') }}#}
		{% endblock %}
	</head>
	<body>
		<header class="color-bg">
			<div class="container">
				{% block header %}
					{% include 'inc/nav_bar.html.twig' %}
				{% endblock %}
			</div>

		</header>
		<main>
			<div class="container mt-2">

				{% for label, messages in app.flashes %}
					{% for message in messages %}
						<div class="notification is-{{ label }} is-light has-text-centered" role="alert">{{ message }}</div>
					{% endfor %}
				{% endfor %}

				
			</div>
			{% block body %}{% endblock %}
		</main>
		{% block footer %}
			<footer class="color-bg">
				<div class="container">
					<p class="content has-text-centered">
						&copy;
						{{ "now"|date("Y") }}
						Digisec
					</p>
				</div>
			</footer>
		{% endblock %}
	</body>
</html>
